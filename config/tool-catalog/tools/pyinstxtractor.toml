id = "pyinstxtractor"
name = "pyinstxtractor"
category = "binary"
summary = "PyInstaller 可执行文件解包（Binary/Reverse）"

usage = """
【FlagDeck 表单】
• 目标 PyInstaller 打包文件绝对路径

【实际调用】
python3 pyinstxtractor.py <filename>

解包结果默认写在**文件同目录** *_extracted/（非任务目录）。
建议使用与打包时相近的 Python 大版本。
"""
mode = "embedded_cli"
featured = true
cwd = "Binary/Reverse/sec-import/pyinstxtractor"

[binary]
command = "python3"
path = ""
resolve = ["system"]

[ui]
icon = "box"
accent = "purple"

[[form.fields]]
id = "file"
type = "text"
label = "PyInstaller 文件路径"
required = true
hint = "被 pyinstaller 打包的 .exe / ELF 绝对路径；输出在旁侧 *_extracted/"

[argv]
template = [
  "{tools_root}/Binary/Reverse/sec-import/pyinstxtractor/pyinstxtractor.py",
  "{file}",
]

[parser]
kind = "none"

[limits]
timeout_millis = 600000
memory_max_bytes = 1073741824
