{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Only the trusted built-in main window can call the two R0 test commands.",
  "windows": ["main"],
  "webviews": ["main"],
  "platforms": ["linux"],
  "permissions": ["allow-ping", "allow-read-fixture"]
}
